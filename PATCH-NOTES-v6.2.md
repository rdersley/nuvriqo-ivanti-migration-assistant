# v6.2 — Migration Test & Validation Centre

Adds evidence-based source-to-Jira validation, service classification, blockers vs failures, manual portal-group tracking, sign-off states, per-service validation report export, and a portfolio validation dashboard. Missing Ivanti workflow metadata is explicitly classified as a source-data blocker rather than a Jira migration failure.
