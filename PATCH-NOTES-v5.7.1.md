# v5.7.1 — frontend TypeScript compile fix

Fixes the two v5.7 build errors:

- `Cannot find name 'JsmDuplicateCleanupResult'`
- implicit `any` for the duplicate-cleanup result callback

No Jira/JSM migration behaviour has been changed.
